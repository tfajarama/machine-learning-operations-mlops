FROM prom/prometheus:latest

ENV GRAFANA_PROMETHEUS_API_TOKEN=${GRAFANA_PROMETHEUS_API_TOKEN}
ENV GRAFANA_PROMETHEUS_USERNAME=${GRAFANA_PROMETHEUS_USERNAME}
ENV GRAFANA_PROMETHEUS_PASSWORD=${GRAFANA_PROMETHEUS_PASSWORD}

COPY prometheus.yml /etc/prometheus/prometheus.yml